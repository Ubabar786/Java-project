Methods/Functions/Behaviours/Routines
- Methods are blocks of code that contains pre-written code that is
when we call a method.

There are two main types of methods:
1) Methods which do not return any data(void type)
2) Methods which can return data like int double char string etc..

Syntax:
datatype methodName (dataType nameOfParameter, dataType name of the Param){
this is where we write the logic

return the results if method type is not void

   int add(int num1, int num2) {
        int result = num1 + num2;

        return result;
        }
===================================
int=> return type of method

ass=>name of the method

(int num1, int num2) => parameters

{        =>body of the method
int result = num1 + num2;

        return result;
}
