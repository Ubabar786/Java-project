package class10;

public class E1StringDemo {
    public static void main(String[] args) {
        // We have created an object from the String class and
        // have stored Cat inside it.
        String obj=new String("Cat");
        // Shortcut to create objects from String class
        String obj2="My Dog"; //String obj2=new String("Dog")
        int len=obj2.length(); //Calling the length method/function/action/behaviour
        System.out.println(len);

    }
}
