package class12;

public class E2CalculatorTester {
    public static void main(String[] args) {
// we have created the object of the Calculator class
        Calculator cal = new Calculator();
        //to call the method using the object of the previous class
        //cal.add();

        // cal.subtract();

        //  cal.multiply(10,20);
       // cal.twoNumbers(300, 40);
        cal.number(456);


    }
}
